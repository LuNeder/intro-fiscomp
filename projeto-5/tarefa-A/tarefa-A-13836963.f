       program palavra
       implicit real*8 (a-h, o-z)
       common x, y, xant, yant
       m = 0

       end program

       real*8 function xi(m)
         implicit real*8 (a-h, o-z)
         common x, y, xant, yant
         x = (2*x)-xant+()
       end function

       real*8 function yi(m)
         implicit real*8 (a-h, o-z)
         common x, y, xant, yant

       end function

