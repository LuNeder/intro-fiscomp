       program c
       implicit real*8 (a-h, o-z)
       r1e = -7
       r2e = 2
       r3e = 9


       end program



       real*8 function f(x)
         implicit real*8 (a-h, o-z)
         f = (x**3)-(4*(x**2))-(59*x)+126
       end function


       real*8 function buscadireta()
         implicit real*8 (a-h, o-z)
         x = -10
         f = 0
         do while (x.lt.)

         end do

        end function
